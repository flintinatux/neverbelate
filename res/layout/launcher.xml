<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:ads="http://schemas.android.com/apk/res/com.madhackerdesigns.neverlate"
	android:layout_width="fill_parent"
	android:layout_height="fill_parent"
	android:orientation="vertical" >

	<LinearLayout
	    android:isScrollContainer="true"
	    android:layout_height="0dip"
	    android:layout_weight="1"
	    android:layout_width="fill_parent"
	    android:orientation="vertical" >
	
		<ImageView
			android:id="@+id/logo"
			android:src="@drawable/main_logo"
			android:layout_gravity="center|top"
			android:layout_height="144dip"
			android:layout_width="144dip"
			android:layout_marginTop="10dip"
			android:layout_marginBottom="10dip"
			android:scaleType="fitCenter" />
			
		<CheckBox 
		    android:id="@+id/btn_enable"
		    android:layout_marginBottom="10dip"
		    android:layout_gravity="center_horizontal"
		    android:layout_height="wrap_content"
		    android:layout_width="wrap_content"
		    android:text="@string/btn_enable_text"
		    android:textSize="16sp" />
		
		<LinearLayout 
		   	android:layout_gravity="center_horizontal"
		    android:layout_height="wrap_content"
		    android:layout_width="wrap_content"
		    android:orientation="horizontal" >
		
			<Button
				android:id="@+id/btn_tutorial"
				android:drawableTop="@drawable/ic_menu_help"
				android:text="@string/btn_tutorial_text"
				style="@style/LauncherButton" />
				
			<Button
				android:id="@+id/btn_configure"
				android:drawableTop="@drawable/ic_menu_preferences"
				android:text="@string/btn_configure_text"
				style="@style/LauncherButton" />
				
		</LinearLayout>
		
		<LinearLayout 
		   	android:layout_gravity="center_horizontal"
		    android:layout_height="wrap_content"
		    android:layout_width="wrap_content"
		    android:orientation="horizontal" >
		
			<Button
				android:id="@+id/btn_share"
				android:drawableTop="@drawable/ic_menu_share"
				android:text="@string/btn_share_text"
				style="@style/LauncherButton" />
				
			<Button
				android:id="@+id/btn_tips"
				android:drawableTop="@drawable/ic_menu_notifications"
				android:text="@string/btn_tips_text"
				style="@style/LauncherButton" />
			
		</LinearLayout>
	
	</LinearLayout>
	
	<com.google.ads.AdView
		android:id="@+id/ad_view"
		android:layout_gravity="center|bottom"
		android:layout_height="wrap_content"
		android:layout_width="wrap_content"
		ads:adUnitId="a14d952e8939105"
        ads:adSize="BANNER"   />
  
</LinearLayout>
